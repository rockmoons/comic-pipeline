# ⚠️ 修改铁律 — 修改任何内容前必读

---

## 🔗 一、关联链路表

改任何一个文件，必须先过一遍：上游谁产出这个字段？下游谁消费这个字段？

```
Agent 1: 故事创作
  产出 → 小说正文（含视觉描述词、情绪节奏）+ 角色ID/场景ID + 章末状态快照JSON
  消费方 → Agent 2（全量，含 character_id / scene_id / emotional_arc_position）

Agent 2: 导演漫改
  产出 → P编号分场剧本 + 演员表（含外观关键词、首次出场、character_id）+ 场景表（含光线/色调、scene_id）+ 章末 scenes_metadata.json
  消费方 → Agent 3（全量，含 JSON block）、Agent 4（全量，含 JSON block）

Agent 3: 美术资产
  产出 → S1演员档案 / S2定妆方案 / S3场景单图 / S4道具库 / S5音色库 / S6 SRT台词本 / @编号↔ID映射表JSON / S0通用兜底资产库
  消费方 → Agent 4（全量，含映射表 JSON）

Agent 4: 分镜生成
  产出 → 素材对应表（含 character_id / scene_id）+ Seedance视频提示词 + 操作指南
  消费方 → libtv / 即梦
```

### 字段级依赖速查

| 改这个文件的这个字段 | Agent 3 哪里会受影响 | Agent 4 哪里会受影响 |
|-------------------|-------------------|-------------------|
| Agent 2 §2.3「人物」拆分规则 | S6 SRT 行数自动增减（§7 逐行分配） | 提示词条数自动增减（§3 逐场生成） |
| Agent 2 §5「转场」类型 | SRT 不受影响（美术 §8.4 第6条：场间不计时） | [起始帧图] 引用的末帧是黑屏，libtv 画布自动处理 |
| Agent 3 §8.4 SRT 时长规则 | — | Agent 4 §3.2 时长基于 SRT 重算，节奏同步变化 |
| Agent 3 @编号分配（§二） | — | §2 素材对应表 + §3.4 @引用规则均依赖编号连续性 |

### JSON 字段级依赖速查（v4.0 新增）

v4.0 各 Agent 需通过 JSON block 传递结构化数据，下游优先解析 JSON，解析失败回退到 Markdown 表格提取（双轨制）。

| JSON 字段 | 产出方 | 消费方 | 用途 | 降级路径 |
|-----------|--------|--------|------|---------|
| `character_id`（如 CHAR_01_陆沉） | Agent 1 | Agent 2/3/4 | 角色主键匹配，替代名字字符比对 | Markdown 演员表「角色ID」列 |
| `scene_id`（如 SCENE_01_北区监狱） | Agent 1 | Agent 2/3/4 | 场景主键匹配，替代名字字符比对 | Markdown 场景表「场景ID」列 |
| `scenes_metadata.json` | Agent 2 | Agent 3/4 | 结构化角色/场景/P编号列表 | 从 Markdown 表格逐行提取 |
| `状态快照 JSON`（chapter/word_count/…） | Agent 1 | 用户/脚本 | 跨章续写上下文 | 用户手动粘贴前文摘要 |
| `@编号↔ID 映射表 JSON` | Agent 3 | Agent 4 | @图片N → character_id/scene_id 双向追溯 | 从素材对应表 Markdown 推断 |
| `本章新增资产卡片 JSON` | Agent 1 | 用户/脚本 | 原子化附录，合并入全局资产文本 | 用户手动统计新增角色 |

---

## 🔧 二、七条修改铁律

### 铁律 1：改前必查关联链
修改任何一个字段前，向上查谁产出、向下查谁消费。确认上下游兼容后再动手。

### 铁律 2：改前必出清单表
每次修改前，必须输出以下五列表格，缺一不可：

| # | 文件 | 位置 | 改什么 | 为什么 | 影响下游 |
|---|------|------|--------|--------|----------|
| 1 | `文件名` | §具体章节 | 具体改动描述 | 变更原因 | 哪些文件受影响、怎么受影响 |

### 铁律 3：改前必出计划等确认
清单表输出后，等待用户明确回复「确认」「改」「动手」等信号后再执行。用户未确认，绝不动手。

### 铁律 4：改后必验证
**禁止**仅凭工具返回的 success/ok 消息就口头宣称"改好了"。每次 Edit/Write 完成后，必须立即用 Read、head 或 grep 抽查关键行，确认改动确实落到了文件里。验证通过后再报告"已确认修改生效"。

### 铁律 5：改后必做全量回归审计（v1.0新增·根治增量 Bug）

每次对任一 Agent prompt 文件做任何修改后，必须对该文件执行以下 9 项全量检查，逐项通过后方可报告"修改完成"。**禁止跳过。禁止缩减。禁止口头声称"应该没问题"。**

```
□ 1. 数字一致性：全文所有块数/版本号/编号范围/时长范围是否与最新规则一致？
   例：旧描述写"五个块"但实际是8个块 → 修正旧文字。
□ 2. 规则↔自检映射（双向）：每一条强制规则是否在对应自检栏有至少 1 个检查项（规则→自检）？
   每一条自检项是否都有对应的显式规则定义（自检→规则）？
   规则有定义但自检无覆盖 → 补全自检项。自检项存在但对应规则不存在 → 补全规则或删除该项。
□ 3. 内部无矛盾：全文搜索是否包含相互冲突的指令？
   例："不添加原文没有的元素" vs "必须嵌入空间比例参照" → 加例外声明。
   例：模板示例含包装语 vs 自检禁止包装语 → 统一。
□ 4. 交叉引用有效：所有"见上方/见 §X.X/见下方 X"的引用是否指向存在的章节？
   引用目标不存在或已移动 → 修正引用路径。
□ 5. 示例不过期：所有示例和代码块是否与当前模板规则一致？
   示例缺新块/用旧格式/无精简版 → 标注废弃或更新示例。
□ 6. 块结构完整：每个模板代码块是否包含当前版本要求的全部输出块？
   模板缺时间分段/精简版/尾帧 → 补全。
□ 7. 字段名一致性：同一个字段在全文中是否使用统一名称？
   例：模板写"前景锚点"但自检写"前景标记" → 统一。
□ 8. 版本标记统一：本次修改涉及的所有规则/模板/自检项的版本号标注是否一致？
   例：同一批改动中有些标 v4.8、有些标 v4.5、有些无版本号 → 统一为本次版本号。
□ 9. 正确示例存在：每条新增或修改的规则/模板，是否至少有一个对应的正确示例？
   无示例时 LLM 自行发挥可能走偏 → 补至少一个正确用法示例。
□ 10. 语义无歧义：每条规则的措辞是否存在可被 LLM 从两种相反方向解读的表述？
   例："是否省略了 X？遗漏的补全"→ LLM 可能理解为"省略了是错的要补"或
   "确认省略了是对的"→ 改为"禁止出现 X·违者删除"。
□ 11. 默认值明确：每个可选字段/条件规则，其默认行为是否显式声明？
   例："可选·未填时默认 X" → 必须写清楚未填时的行为。未声明默认值的可选字段
   视为规则不完整。
```

这 11 项检查不是建议，是**铁律**。每次改动完成后必须逐项执行并报告通过状态。未执行全量回归审计的修改视为未完成。

---

### 铁律 6：用户反馈质量问题 → 自动追溯 Prompt 根因（v1.0新增）

当用户报告输出内容存在质量问题（格式缺失/内容错误/逻辑矛盾/跨文件不一致等），**禁止**直接修输出文件。必须先回答三个问题：

```
① 这个问题的根因在哪个 Prompt 文件的哪条规则？
② 是规则缺失、规则冲突、还是规则未被遵守？
③ 修复方案是先修 Prompt 再修输出，还是只修 Prompt 下次生效？
```

回答完这三个问题后，等待用户确认再动手。输出文件是症状，Prompt 才是病因——只修症状不修病因等于白修。

### 铁律 7：改了一个 Prompt → 自动扫描下游是否需要同步（v1.0新增）

当修改任一 Agent 的 Prompt 文件后，在铁律 5 全量审计的基础上，追加一步跨文件检查：

```
□ 下游文件是否引用了被修改的字段/规则/格式？
   → 检查关联链路表（§一）中该字段的下游消费方
   → 逐一下游文件搜索，确认是否需要对应更新
   → 需要更新但未更新 → 列出差异并建议修改
```

禁止改完导演不管美术、改完美术不管分镜。

### 铁律 8：跑流程前必须先收集基础信息（v1.0新增）

当用户说「跑流程」时，**禁止**直接开始生成。必须先向用户确认以下信息，缺一不可：

```
1. 故事构思/选题：[用户输入 / 或回复"你定"由 Agent 自行拟定·二选一必填]
2. 视觉风格：[默认 CG国漫·用户可覆写]
   **询问时必须列出全部 50 套可选风格（来源：04_分镜生成_v3.0.md §3.5 Style Token 表）**，
   分为 3D(11套)/2D(30套)/真人(5套)/定格(5套) 四类展示，不可口头概括。
3. 章节数：[默认 1 章·用户可覆写]
4. 飞书凭证：[可选·如已设置环境变量则跳过。需 FEISHU_APP_ID + FEISHU_APP_SECRET]
```

确认后方可执行。用户未明确选项 1 时，默认由 Agent 自行拟定。

### 铁律 9：改 Prompt 必须同步审查外部防线（v1.0新增）

每次修改 Prompt 文件后，在铁律 5 全量审计和铁律 7 跨文件扫描的基础上，追加两道防线同步检查：

```
防御层 2（外部校验脚本）：
  □ 新增/修改的格式/结构/数量规则，src/validators/ 中是否有对应的 Python 检查函数？
    无 → 补写。只补机械检查（计数/格式匹配/字段存在），语义检查不重复。

防御层 3（下游 JSON 解析）：
  □ JSON block 的字段名/结构是否变更？
    是 → 下游 Agent 的 JSON 解析路径是否需要更新？
    否 → 标记"无变更·跳过"。
```

**审计必须可见**：每项检查的结果必须显式输出到对话中，格式如下。禁止口头声称"已检查通过"而不展示具体数据。

```
🔍 全量回归审计 — [文件名]
□ 1. 数字一致性 → [✅/❌] [具体数据]
□ 2. 规则↔自检映射（双向） → [✅/❌] [规则→自检覆盖数 vs 自检→规则覆盖数]
□ 3. 内部无矛盾 → [✅/❌] [报告是否有冲突·列出行号]
□ 4. 交叉引用有效 → [✅/❌] [引用数·全部有效/无效项]
□ 5. 示例不过期 → [✅/❌] [示例数·是否与当前模板一致]
□ 6. 块结构完整 → [✅/❌] [应有 vs 实有]
□ 7. 字段名一致性 → [✅/❌] [是否有同义不同名·列出]
□ 8. 版本标记统一 → [✅/❌] [本次改动版本号是否全部一致]
□ 9. 正确示例存在 → [✅/❌] [新增规则数 vs 示例数]
□ 10. 语义无歧义 → [✅/❌] [无可双向解读的表述/歧义项列出行号]
□ 11. 默认值明确 → [✅/❌] [可选字段数 vs 已声明默认值数]
```

---

## 📋 三、改动清单模板

每次修改前复制以下模板：

```
| # | 文件 | 位置 | 改什么 | 为什么 | 影响下游 |
|---|------|------|--------|--------|----------|
| ① | `XX_xxx_v1.0.md` | §章节名 第N行 | 具体改动 | 原因 | 影响谁 |

---
## 不改的文件
| 文件 | 原因 |
|------|------|
| `XX` | 无问题/下游自动受益 |

---
确认后执行。
```

---

## 🎯 四、目标平台

当前目标平台：**libtv**（LiblibAI 出品，集成 Seedance 2.0 + 节点式工作流 + Agent CLI）
- @编号 对应 libtv 主体库节点
- 末帧传递由 libtv 节点画布自动处理
- Agent 4 的 C 阶段手动截帧步骤在 libtv 中不适用

---

## 📦 六、Agent 间数据传递规范（v4.0）

### 6.1 传递总则：JSON + Markdown 双轨制

所有 Agent 产出同时包含两份等效数据：
- **JSON block**（` ```json ``` ` 代码块包裹）：机读优先路径
- **Markdown 表格/正文**：人类阅读 + JSON 解析失败时的降级路径

下游 Agent 收到上游产出后：
1. 先尝试 `JSON.parse` 解析 JSON block
2. 解析成功 → 直接使用 JSON 数据做后续校验和引用
3. 解析失败 → 回退到从 Markdown 表格/正文中提取（降级路径）

### 6.2 主键 ID 体系

所有角色、场景使用全局唯一 ID，格式固定：

| ID 类型 | 格式 | 示例 | 首次分配者 |
|---------|------|------|-----------|
| 角色ID | `CHAR_NN_NAME` | `CHAR_01_陆沉` | Agent 1（故事创作） |
| 场景ID | `SCENE_NN_NAME` | `SCENE_01_北区监狱` | Agent 1（故事创作） |

> **场景ID vs P编号的职责边界**：`scene_id`（SCENE_NN_NAME）是**全局世界观宏观场景资产库**的主键——标识一个物理地点（如"天玄门大殿"），跨章复用不重置。`P编号`（P01, P02_A...）是**分镜场次标号**，由导演 Agent 按每章独立分配，标识一个具体的镜头机位和时刻。美术指导在输出 @编号↔ID 映射时，必须同时关联 `scene_id`（场景名）与当前剧本的 `P编号`（场次），两者各自独立、不可替代、不可混淆。
| 道具ID | `PROP_NN_NAME` | `PROP_01_火球符` | Agent 3（美术指导） |

下游 Agent 沿用上游 ID，不得重新编号。新增角色/场景由当前 Agent 按格式续编（如 Agent 2 新增角色 → `CHAR_05_林若雪`）。

### 6.3 名称匹配规则

下游做名称匹配时：
- **优先路径**：通过 `character_id` / `scene_id` 主键精确比对
- **降级路径**：若 JSON 不可用，改用自然语言**包含匹配（contains）**，容差 ±5 个字符以内的标点/空格/大小写差异。禁止字符级精确匹配（exact match）

### 6.4 @编号↔ID 映射表

Agent 3（美术指导）在所有资产生成完毕后，末尾必须输出 `@编号↔ID` 映射表 JSON：

```json
{
  "@image_mapping": [
    {"@image": "@图片1", "type": "actor", "id": "CHAR_01_陆沉", "name": "陆沉"},
    {"@image": "@图片5", "type": "scene", "id": "SCENE_01_北区监狱", "name": "北区监狱"},
    {"@image": "@图片12", "type": "prop", "id": "PROP_01_火球符", "name": "火球符"}
  ],
  "@audio_mapping": [
    {"@audio": "@音频1", "id": "CHAR_01_陆沉", "name": "陆沉"}
  ]
}
```

Agent 4（分镜师）直接读取此 JSON 做 @引用校验，不需自行推断。

### 6.5 跨章状态传递

多章处理时，上游 Agent 在每章产出末尾追加「累计状态摘要」JSON：

```json
{
  "chapter": 3,
  "cumulative_characters": 5,
  "cumulative_scenes": 3,
  "last_at_image": "@图片14",
  "last_at_audio": "@音频5",
  "new_in_this_chapter": {
    "characters": ["CHAR_05_林若雪"],
    "scenes": ["SCENE_03_暗鸦森林"]
  }
}
```

下游 Agent 读取上一章的累计状态作为本章起始状态，实现无状态 LLM 的「伪有状态」续写。

---

## 📋 七、跨章状态快照规范（v4.0）

Agent 1（故事创作）每章正文输出完毕后，在 `【第X章 完】` 之前，必须输出以下 JSON 快照：

```json
{
  "chapter": 5,
  "word_count": 1050,
  "cumulative_words": 5200,
  "new_character_ids": ["CHAR_05_林若雪"],
  "new_scene_ids": ["SCENE_03_暗鸦森林"],
  "unresolved_threads": [
    "暗鸦森林深处的神秘呼唤源头未揭晓",
    "林若雪的真实身份尚未暴露"
  ],
  "emotional_arc_position": "中段·悬疑虐心期",
  "last_hook_type": "双重钩子——新敌人出现 + 主线威胁升级",
  "blueprint_progress": "第5章/20章（25%）"
}
```

快照字段约束：
- `unresolved_threads`：最多 3 条，超过 3 条时 Agent 须在下章主动收束至少 1 条
- `emotional_arc_position`：三选一——「前期·轻松搞笑期」「中段·悬疑虐心期」「后期·热血燃爽期」
- `last_hook_type`：五选一——「新敌人出现」「危机升级」「双重钩子」「主线威胁引入」「重大转折」
- `blueprint_progress`：当前进度百分比，用于 §4.2 阶段自检判断进度是否偏离蓝图
- 用户将此快照 + 上一章正文喂回 LLM，即可无状态续写——彻底解决上下文窗口被历史章节挤爆的问题

---

## 🚀 五、跑流程

当用户说 **「跑流程」** 时，执行完整 4-Agent 流水线。

### 5.1 执行方式

**直接生成，不调 API。** Agent 自身就是 LLM——读取对应的 prompt 文件，按模板规则直接输出内容。禁止使用 src/orchestrator.py、src/pipeline_runner.py、Bash 调 Python 子进程、或任何外部 API 做 LLM 生成。**飞书 API 调用（数据写入）和 .md 文件写入不受此限制。**

### 5.2 输出方式

**展示优先，文件后置，飞书跟随。** 每个 Agent 生成完成后立即展示全文。全部 4 个 Agent 展示完毕后一次性写入 .md，再写入飞书多维表格。

| 阶段 | 动作 | 时机 |
|------|------|------|
| 展示 | 输出**完整全文**到对话 | 每个 Agent 生成完立刻展示 |
| 写入 .md | 写到 output 目录 | 4 个 Agent 全部展示完之后·一次性写入 |
| 写入飞书 | 🆕 写入飞书多维表格 | .md 写入完成后执行 |

禁止：缩写、省略、"见文件"、只展示摘要、边展示边写文件。4 个 Agent 逐个展示期间不执行任何文件写入（前置创建目录除外）。

**飞书环境变量**：需设置 `FEISHU_APP_ID` + `FEISHU_APP_SECRET`（见铁律 8）。若未设置则跳过飞书写入，不阻断流程。
- 使用 `src/utils/feishu_writer.py` 写入
- 写入前 Agent 必须从生成文本中提取结构化 dict（首章 `ensure_tables` 建表·后续 `use_bitable` 复用）
- 写入失败不阻断流程·打印错误继续

### 5.3 文件命名规则

```
output/<session_id>/
├── 故事创作Agent_第1章_[四字标题].md
├── 导演漫改Agent_第1章_分场剧本.md
├── 美术资产Agent_第1章_资产库.md
└── 分镜生成Agent_第1章_视频提示词.md
```

| 命名部分 | 含义 | 示例 |
|---------|------|------|
| 职能名 | 哪个 Agent | `故事创作Agent` / `导演漫改Agent` |
| 章节 | 第几章 | `第1章` |
| 内容类型 | 文件用途 | `分场剧本` / `资产库` / `视频提示词` |
| 书名（仅故事） | 四字章节标题 | `峡谷追猎` |

**禁止使用编号命名**（如 story_ch01.txt、agent1_output.txt 等）。文件名一眼必须看出：谁生成的、第几章、里面是什么。

### 5.4 执行步骤

逐个 Agent 执行，每个 Agent 开始前必须告知用户。全部展示完毕后统一写入文件。

先创建 output 目录——session_id 格式：`YYYYMMDD_故事标题缩写`。标题缩写规则：取书名前 4 个汉字，4 字以内取全名（如《火星飙车》→ `20260702_火星飙车`，《破土》→ `20260702_破土`）。

```
→ 创建 output/YYYYMMDD_故事标题缩写/ 目录

🔴 开始故事创作Agent...
   → 生成 → 前台展示全文

🔴 开始导演漫改Agent...
   → 生成 → 前台展示全文

🔴 开始美术资产Agent...
   → 生成 → 前台展示全文

🔴 开始分镜生成Agent...
   → 生成 → 前台展示全文

→ 4个全部展示完毕 → 批量写入 4 个 .md 文件
→ 若 FEISHU_APP_ID 已设置 → 写入飞书多维表格（首次自动创建·后续追加）
```

### 5.5 要求

- 执行前必须确认故事选题/视觉风格/章节数（见铁律 8）·用户未指定选题时由 Agent 自行拟定
- 每个 Agent 输出必须**完整详细**，不省略任何规定板块
- 视觉风格默认 **CG国漫**
- 最终结果作为用户审核依据
